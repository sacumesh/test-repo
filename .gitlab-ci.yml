stages:
  - build

variables:
  GIT_STRATEGY: none  # Prevent GitLab from trying to pull its own repo

build:
  stage: build
  script:
    - git clone https://github.com/<USERNAME>/<REPO>.git source
    - cd source
    - mvn clean package -DskipTests  # Example for Java
